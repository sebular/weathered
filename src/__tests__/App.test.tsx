import React from 'react'
import { render, screen } from '@testing-library/react'

import { App } from '../App'

describe('<App />', () => {
  test('should do things', async () => {
    // const app = render(<App />)
    // const incrementButton = await app.findByTestId('increment-button')
    // expect(incrementButton.innerHTML).toEqual('count is: 0')
    // incrementButton.click()
    // expect(incrementButton.innerHTML).toEqual('count is: 1')
  })
})